import SwiftUI

struct FinanceWidgetView: View {
    var body: some View {
        Image(systemName: "dollarsign.circle.fill")
            .font(.system(size: 20))
            .foregroundStyle(.white)
    }
}
